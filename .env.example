# Server configuration
PORT=3000
NODE_ENV=development

# Database
DATABASE_URL=./data/place-a-bet.db

# CORS (for development, set to '*' or specific origin)
CORS_ORIGIN=*

# Host PIN (4 digits) - used for host authentication
# Change this and restart the app to reset the PIN
HOST_PIN=1234
