# Server configuration
PORT=3001
NODE_ENV=production

# Database (optional - defaults to server/data/place-a-bet.db)
# Only set this if you want to use a custom location
# DATABASE_URL=/custom/path/to/database.db

# CORS (for production, keep as '*' or set to specific origin)
CORS_ORIGIN=*

# Host PIN (4 digits) - used for host authentication
# IMPORTANT: Change this to a secure 4-digit number!
HOST_PIN=1234
